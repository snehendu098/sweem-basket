specVersion: 1.0.0
description: MetaMorpho (Morpho Blue) ERC-4626 vaults on Base Sepolia — TVL,
  flows and share-price history for APY derivation.
repository: https://github.com/
schema:
  file: ./schema.graphql
indexerHints:
  prune: never
dataSources:
  - kind: ethereum
    name: MetaMorphoFactory
    network: base-sepolia
    source:
      address: "0xA9c3D3a366466Fa809d1Ae982Fb2c46E5fC41101"
      abi: MetaMorphoFactory
      startBlock: 9315415
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.9
      language: wasm/assemblyscript
      file: ./src/factory.ts
      entities:
        - Vault
      abis:
        - name: MetaMorphoFactory
          file: ./abis/MetaMorphoFactory.json
        - name: MetaMorpho
          file: ./abis/MetaMorpho.json
        - name: ERC20
          file: ./abis/ERC20.json
      eventHandlers:
        - event: CreateMetaMorpho(indexed address,indexed address,address,uint256,indexed
            address,string,string,bytes32)
          handler: handleCreateMetaMorpho
templates:
  - kind: ethereum
    name: MetaMorpho
    network: base-sepolia
    source:
      abi: MetaMorpho
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.9
      language: wasm/assemblyscript
      file: ./src/vault.ts
      entities:
        - Vault
        - VaultHourlySnapshot
        - VaultDeposit
        - VaultWithdraw
      abis:
        - name: MetaMorpho
          file: ./abis/MetaMorpho.json
        - name: ERC20
          file: ./abis/ERC20.json
      eventHandlers:
        - event: Deposit(indexed address,indexed address,uint256,uint256)
          handler: handleDeposit
        - event: Withdraw(indexed address,indexed address,indexed address,uint256,uint256)
          handler: handleWithdraw
        - event: AccrueInterest(uint256,uint256)
          handler: handleAccrueInterest
        - event: UpdateLastTotalAssets(uint256)
          handler: handleUpdateLastTotalAssets
        - event: SetFee(indexed address,uint256)
          handler: handleSetFee
        - event: SetCurator(indexed address)
          handler: handleSetCurator

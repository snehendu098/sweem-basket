{
  "name": "morpho-blue-base",
  "version": "0.1.0",
  "description": "MetaMorpho (Morpho Blue) vault subgraph for Base / Base Sepolia",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build --network base-sepolia",
    "deploy": "graph deploy morpho-blue-base --network base-sepolia"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.38.1"
  }
}

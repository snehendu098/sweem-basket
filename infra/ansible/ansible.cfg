[defaults]
inventory = inventory.ini
host_key_checking = False
retry_files_enabled = False
# ansible-core >= 2.13 renders yaml through the default callback.
result_format = yaml

[ssh_connection]
pipelining = True
